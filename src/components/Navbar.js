import './Nav.css';
import './CovidDetails.css';
import Input from './Input';

const Navbar = () => (
  <>
    <header>
      <nav>
        <Input />
      </nav>
    </header>
  </>

);

export default Navbar;
